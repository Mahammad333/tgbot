# Core Bot Packages
python-telegram-bot==13.7
requests==2.26.0

# Flask for Webhook or additional features (adjust based on your usage)
flask==3.0.3

# gTTS for text-to-speech (if used)
gtts==2.2.3

# Compatible Click version (adjusted to avoid conflicts)
click>=8.1.3,<8.2.0

# mailru-im-bot (Optional, if needed)
# If this is not critical, you might want to remove it to avoid conflicts
# mailru-im-bot==0.0.21
